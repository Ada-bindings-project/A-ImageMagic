project ImageMagick is
   for Create_Missing_Dirs use "True";
   for Source_Dirs use ("src/**");
   for Object_Dir use ".obj";
   for Library_Name use "AImageMagick";
   for Library_Dir use "lib";

end ImageMagick;

